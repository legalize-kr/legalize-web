{
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && node scripts/build-search-index.mjs",
    "prepare": "cd ../legalize-pipeline && python3 -m images export --output ../legalize-web/public/data/images/ --sharded && cd ../legalize-web && ln -sf /Users/reserve/Workspace/legalize-kr/.cache/images local-images 2>/dev/null || true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0",
    "pagefind": "^1.4.0"
  }
}
